name: Linux RDP Bridge KeepAlive

on:
  repository_dispatch:
    types: [start-rdp-bridge]
  workflow_dispatch:
    inputs:
      ip:
        description: 'Target Tailscale IP'
        required: true
      username:
        description: 'Target Windows Username'
        required: true
      password:
        description: 'Target Windows Password'
        required: true

jobs:
  rdp-bridge:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Add Tailscale Repository & Install Packages
        run: |
          # 1. Add Tailscale GPG Key & Repo
          curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/noble.noarmor.gpg | sudo tee /usr/share/keyrings/tailscale-archive-keyring.gpg >/dev/null
          curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/noble.tailscale-keyring.list | sudo tee /etc/apt/sources.list.d/tailscale.list

          # 2. Update APT and install packages (FreeRDP 3 / FreeRDP 2 fallback, Xvfb, Tailscale)
          sudo apt-get update
          sudo apt-get install -y freerdp3-x11 || sudo apt-get install -y freerdp2-x11
          sudo apt-get install -y xvfb tailscale

      - name: Authenticate Tailscale
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=linux-rdp-bridge

      - name: Resolve IP & Credentials
        run: |
          TARGET_IP="${{ github.event.client_payload.ip || inputs.ip }}"
          TARGET_USER="${{ github.event.client_payload.username || inputs.username }}"
          TARGET_PASS="${{ github.event.client_payload.password || inputs.password }}"

          echo "TARGET_IP=$TARGET_IP" >> $GITHUB_ENV
          echo "TARGET_USER=$TARGET_USER" >> $GITHUB_ENV
          echo "TARGET_PASS=$TARGET_PASS" >> $GITHUB_ENV

          echo "::add-mask::$TARGET_PASS"

      - name: Run Persistent RDP Session inside XVFB
        run: |
          echo "Connecting headless RDP session to $TARGET_IP..."

          # Start Virtual X Server
          Xvfb :99 -screen 0 1920x1080x24 &
          export DISPLAY=:99
          sleep 2

          # Launch FreeRDP session in background
          xfreerdp /v:"$TARGET_IP" \
                   /u:"$TARGET_USER" \
                   /p:"$TARGET_PASS" \
                   /size:1920x1080 \
                   /cert-ignore \
                   +gfx-h264 \
                   /network:auto \
                   /sound:sys:alsa &
          
          RDP_PID=$!
          echo "RDP Client running under PID: $RDP_PID"

          # Monitor Process: loop until disconnect
          while kill -0 $RDP_PID 2>/dev/null; do
            echo "[$(date)] Linux Bridge actively maintaining GUI session on $TARGET_IP..."
            sleep 15
          done

          echo "RDP connection closed or overridden. Terminating action cleanly."
